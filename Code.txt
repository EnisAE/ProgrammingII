

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(){

    int idList[] = {20191706007,20201706021,20191706009};
    char passList[][10]={"AhmetB","EnisEr","UmutSe"};
    int votedAlready[]={0,0,0};

    char pass[10];
    int id;
    char vote;

    int n = 0;
    int y = 0;
    int totalVote = 0;
    float element=sizeof(passList)/sizeof(*passList);



        while( y < element/2 && n < element/2 && n+y < element){

        int flag = 0;
        printf("Insert your ID please: \n");
        scanf("%d",&id);

        printf("Insert your PASS please: \n");
        scanf("%s",pass);



            for(int i = 0; i < 3; i++){


                if(id == idList[i] && (strcmp(pass,passList[i])) == 0 && 1 != votedAlready[i]){
                    flag=1;
                    votedAlready[i] = 1;
                }


            }

            if(flag==1){
            totalVote++;
                printf("Vote please (Y/N): \n");
                scanf(" %c", &vote);

                if(vote=='N' || vote=='n'){

                            n++;

                        }

                else if(vote=='Y' || vote=='y'){

                            y++;

                        }
            }

            else if(flag==0) {
                printf("Please make sure your ID is a valid one or you did not vote yet... \n\n");

                continue;
            }

            printf("total vote:%d \n",totalVote);
    }

    if(y>n){
        printf("The group decision is YES.");
    }

    else{
        printf("The group decision is NO.");
    }
    return 0;
}
